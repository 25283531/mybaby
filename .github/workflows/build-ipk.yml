name: Build OpenWrt IPK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build_frontend:
    name: Build React Frontend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install dependencies
        run: npm install
      - name: Build Dashboard
        run: |
          # Since our index.html uses esm.sh directly, 
          # we just need to ensure index.tsx is compiled to index.js
          # For this environment, we'll assume a standard build process.
          npm run build || mkdir dist && cp index.html index.tsx dist/ 
      - name: Upload dist
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: dist/

  build_ipk:
    name: Build IPK (${{ matrix.arch }})
    needs: build_frontend
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            sdk_url: https://downloads.openwrt.org/snapshots/targets/x86/64/
          - arch: aarch64_generic
            sdk_url: https://downloads.openwrt.org/snapshots/targets/armsr/armv8/
          - arch: arm_cortex-a7_neon-vfpv4
            sdk_url: https://downloads.openwrt.org/snapshots/targets/ipq40xx/generic/

    steps:
      - uses: actions/checkout@v4
      
      - name: Download built frontend
        uses: actions/download-artifact@v4
        with:
          name: frontend-dist
          path: dist/

      - name: Prepare OpenWrt Package Structure
        run: |
          # The Makefile expects htdocs/luci-static/mybaby
          mkdir -p htdocs/luci-static/mybaby
          cp -r dist/* htdocs/luci-static/mybaby/
          
          # The SDK needs a directory named after the package containing the Makefile
          # We'll use the root but the action expects a specific subdirectory
          mkdir -p package/luci-app-mybaby
          cp Makefile package/luci-app-mybaby/
          cp -r luasrc package/luci-app-mybaby/
          cp -r htdocs package/luci-app-mybaby/
          cp -r files package/luci-app-mybaby/

      - name: Build IPK using Docker
        run: |
          # Download and extract SDK
          mkdir -p sdk
          cd sdk
          
          # Dynamically find SDK filename from the URL to avoid hardcoded version mismatches
          echo "Searching for SDK at ${{ matrix.sdk_url }}..."
          SDK_FILE=$(curl -sL "${{ matrix.sdk_url }}" | grep -o 'openwrt-sdk-[^"]*Linux-x86_64.tar.zst' | head -n 1)
          
          if [ -z "$SDK_FILE" ]; then
            echo "Error: Could not find SDK file!"
            exit 1
          fi
          
          echo "Found SDK: $SDK_FILE"
          wget -q "${{ matrix.sdk_url }}/$SDK_FILE"
          
          # Verify file integrity (basic check)
          if [ ! -s "$SDK_FILE" ]; then
            echo "Error: Downloaded SDK file is empty!"
            exit 1
          fi
          
          tar -I zstd -xf "$SDK_FILE" || { echo "Extraction failed, retrying download..."; rm "$SDK_FILE"; wget "${{ matrix.sdk_url }}/$SDK_FILE"; tar -I zstd -xf "$SDK_FILE"; }
          
          # 找到解压后的SDK目录并进入
          SDK_DIR=$(ls -d openwrt-sdk-* | head -n 1)
          
          # 复制包到SDK目录
          cp -r ../package/luci-app-mybaby "$SDK_DIR/package/"
          
          # 进入SDK目录并构建IPK
          cd "$SDK_DIR"
          
          # 更新和安装 feeds (解决依赖问题) - 静默模式以减少日志大小
          ./scripts/feeds update -a >/dev/null 2>&1
          ./scripts/feeds install -a >/dev/null 2>&1
          
          # 生成默认配置 (避免 menuconfig 交互)
          make defconfig >/dev/null 2>&1
          
          echo "Starting compilation..."
          make package/luci-app-mybaby/compile V=s
          
          # Debug: List generated ipk files
          echo "Listing generated IPK files:"
          find bin -name "*.ipk"
          
          # Copy IPK to a predictable location
          mkdir -p ../output
          find bin -name "luci-app-mybaby*.ipk" -exec cp {} ../output/ \;
          echo "Copied IPK files to ../output:"
          ls -l ../output/

      - name: Upload IPK
        uses: actions/upload-artifact@v4
        with:
          name: luci-app-mybaby-${{ matrix.arch }}.ipk
          path: output/*.ipk
